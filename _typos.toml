[default.extend-words]
# `astroid` is the name of a python library, and it is used in pylint configuration. should not be corrected to asteroid
astroid = "astroid"

[files]
extend-exclude = [".copier-answers.yml"] # this is an autogenerated file, and sometimes the commit sha gets confused as being a word
